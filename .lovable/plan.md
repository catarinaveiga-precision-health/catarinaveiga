

## Plan: Add ferritina page to sitemap.xml

Add the `/ferritina-baixa-sintomas` URL entry to `public/sitemap.xml` with `monthly` changefreq and `0.8` priority, placed among the other content pages.

### Change
**`public/sitemap.xml`** — Insert new `<url>` block after the `/blog` entry (before `/aviso-legal`).

